// ============================================================
//   ДОМАШНЕЕ ЗАДАНИЕ: Variables, Data Types, Scopes,
//   Hoisting, TDZ, typeof, Operators
// ============================================================
//
//   Как выполнять:
//   1. Для каждого задания СНАЧАЛА запиши ответ в комментарии
//   2. ПОТОМ запусти код и проверь себя
//   3. Если ошибся — объясни себе почему
//
// ============================================================


// ============================================================
// Task 1 — Declaration & Initialization + Data Types
// ============================================================
/*
   Объяви подходящие переменные и присвой им значения:
   1) Переменная для хранения даты создания файла
   2) Переменная для хранения имени пользователя
   3) Переменная для хранения количества ошибок
   4) Переменная для хранения статуса теста (пройден или нет)

   — Используй подходящие типы данных для каждой переменной
   — Выведи все переменные в консоль
*/

// твой код здесь


// ============================================================
// Task 2 — typeof
// ============================================================
/*
   Определи тип каждой переменной с помощью typeof
   и выведи результат в консоль.

   — Для каждой переменной выведи строку вида:
     "Тип переменной a: number"
   — Обрати внимание на результат typeof для null —
     это известная особенность JS, объясни её в комментарии
*/

let a = 42;
let b = "hello";
let c = true;
let d = null;
let e = undefined;
let f = { name: "Nat" };
let g = [1, 2, 3];

// твой код здесь


// ============================================================
// Task 3 — Hoisting (var)
// ============================================================
/*
   Что выведет этот код?
   Запиши ответ в комментарии ДО запуска, затем проверь.

   console.log(x);
   var x = 5;
   console.log(x);

   Ожидаемый результат:
   // строка 1:
   // строка 2:

   — Объясни в комментарии почему первый console.log
     не выдаёт ошибку
*/

// раскомментируй и проверь:
// console.log(x);
// var x = 5;
// console.log(x);


// ============================================================
// Task 4 — Temporal Dead Zone (let/const)
// ============================================================
/*
   Что выведет этот код?
   Запиши ответ в комментарии ДО запуска, затем проверь.

   console.log(myVar);
   let myVar = 10;

   Ожидаемый результат:
   // строка 1:

   — Объясни чем поведение let отличается от var
*/

// раскомментируй и проверь:
// console.log(myVar);
// let myVar = 10;


// ============================================================
// Task 5 — Scopes
// ============================================================
/*
   Предскажи что выведет код.
   Запиши ответ в комментарии ДО запуска, затем проверь.

   Ожидаемый результат:
   // строка 1:
   // строка 2:

   — Почему выводятся разные значения?
     Объясни в комментарии.
*/

let result = "global";

function showResult() {
  let result = "local";
  console.log(result);
}

showResult();
console.log(result);


// ============================================================
// Task 6 — Arithmetic Operators
// ============================================================
/*
   Вычисли результат вручную, запиши в комментарии,
   затем проверь в консоли.

   Ожидаемый результат:
   // 1)
   // 2)
   // 3)
   // 4)
   // 5)
   // 6)

   — Для каждого выражения используй шаблонную строку вида:
     `10 + 3 = ${a + b}`
*/

let numA = 10;
let numB = 3;

// твой код здесь


// ============================================================
// Task 7 — Logical Operators: truthy/falsy миксы
// ============================================================
/*
   Предскажи результат каждого выражения.
   Запиши ответ в комментарии ДО запуска, затем проверь.
*/

// Группа 1 — цепочки || (возвращает первое truthy значение)
/*
   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
   // строка 4:
*/
console.log(0 || "" || null || "finally!");
console.log(null || 0 || undefined || false);
console.log("" || 0 || false || null);
console.log(null || false || "" || 0 || "last resort");

// Группа 2 — цепочки && (возвращает первое falsy значение)
/*
   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
*/
console.log(1 && "ok" && true && 42);
console.log(1 && 0 && "never" && true);
console.log("hello" && 42 && null && "world");

// Группа 3 — ! внутри выражения
/*
   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
   // строка 4:
*/
console.log(!"" && !null && !0);
console.log(!null && !undefined && !"");
console.log(!0 || !"hello");
console.log(!1 && !"");

// Группа 4 — && и || вместе
/*
   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
   // строка 4:

   — Объясни в комментарии: почему && выполняется раньше ||?
*/
console.log("hello" && 0 || "world");
console.log(null || "ok" && false);
console.log(0 || null || "yes" && 42);
console.log("" && "a" || null || "b" && "c");

// Группа 5 — &&, || и ! все вместе
/*
   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
   // строка 4:
   // строка 5:
   // строка 6:

   — Объясни: выражения всегда возвращают boolean или нет?
*/
console.log(!null && ("" || "fallback"));
console.log(!0 && (null || "result"));
console.log(!"" && !0 || null);
console.log(!false && (0 || "") || "default");
console.log(!(null || 0) && ("" || "done"));
console.log(!undefined && !null && (0 || false || "last"));


// ============================================================
// Task 8 — Конкатенация vs Шаблонные строки
// ============================================================
/*
   Составь строку вида:
   "Привет! Меня зовут Natasha Ivanova. Я QA Engineer с опытом 3 года."

   — Первый раз через конкатенацию (+)
   — Второй раз через шаблонную строку (` `)
   — Выведи оба результата и убедись что они одинаковы
*/

let firstName = "Natasha";
let lastName = "Ivanova";
let role = "QA Engineer";
let experience = 3;

// твой код здесь


// ============================================================
// Task 9 — Унарный + и -
// ============================================================
/*
   Что выведет код? Запиши ответ в комментарии ДО запуска,
   затем проверь.

   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
   // строка 4:
   // строка 5:
   // строка 6:
   // строка 7:
   // строка 8:
   // строка 9:

   — Объясни в комментарии: зачем нужен унарный +
     и когда его используют?
*/

let strA = "5";
let strB = "3";

console.log(strA + strB);
console.log(+strA + +strB);
console.log(+strA - +strB);
console.log(-strA);
console.log(+"hello");
console.log(+true);
console.log(+false);
console.log(+null);
console.log(+undefined);


// ============================================================
// Task 10 — Неявная типизация (Type Coercion)
// ============================================================
/*
   Предскажи результат каждого выражения.
   Запиши ответ в комментарии ДО запуска, затем проверь.

   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
   // строка 4:
   // строка 5:
   // строка 6:
   // строка 7:
   // строка 8:
   // строка 9:
   // строка 10:

   — Объясни: почему "5" + 1 и "5" - 1 дают разные результаты?
*/

console.log("5" + 1);
console.log("5" - 1);
console.log("5" * 2);
console.log(true + 1);
console.log(false + 1);
console.log(null + 1);
console.log(undefined + 1);
console.log("" + 1);
console.log(" " + 1);
console.log([] + 1);


// ============================================================
// Task 11 — Финальный микс (== vs ===, !!, type coercion)
// ============================================================
/*
   Предскажи результаты.
   Запиши ответ в комментарии ДО запуска, затем проверь.

   Ожидаемый результат:
   // строка 1:
   // строка 2:
   // строка 3:
   // строка 4:
   // строка 5:
   // строка 6:
   // строка 7:
   // строка 8:
   // строка 9:
   // строка 10:

   — Объясни разницу между == и === на примере строк 3 и 4
   — Объясни что делает !! и зачем его используют
   — Объясни почему null == undefined, но null !== undefined
*/

let checkX = "10";
let checkY = 5;

console.log(checkX > checkY);
console.log(+checkX > checkY);
console.log(checkX == 10);
console.log(checkX === 10);
console.log(!!0);
console.log(!!1);
console.log(!!"");
console.log(!!"hello");
console.log(null == undefined);
console.log(null === undefined);
